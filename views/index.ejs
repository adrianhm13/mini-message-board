<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>
  <body>
    <h1 class="title"><%= title %></h1>
    <div class="messages-container">
      <% if (messages) { %> <% messages.forEach(message => { %>
      <div class="message">
        <h3 class="author"><%= message.user %></h3>
        <p><%= message.text %></p>
        <p class="time"><%= message.added %></p>
      </div>
      <% }) %> <% } else { %>
      <p>No messages in the board</p>
      <% } %>
    </div>

    <a href="/new">Add a new message</a>
  </body>
</html>
